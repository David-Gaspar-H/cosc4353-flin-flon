import { Typography, Grid2 } from "@mui/material";

const Dashboard = () => {
	return (
		<Grid2>
			<Typography variant="h3">You are normal user :)</Typography>
		</Grid2>
	);
};

export default Dashboard;
